{
  "name": "@daneroo/foundations",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "lerna": "^3.5.1",
    "jest": "^23.6.0",
    "standard": "^12.0.1"
  },
  "scripts": {
    "clean":"lerna clean --yes && find packages -name coverage",
    "unit":"lerna run unit --stream  -- -- --verbose",
    "test": "lerna run test"
  },
  "standard": {
    "env": {
      "jest": true
    }
  }
}
